/**
 * STARDUST WORKERS - Unified Export
 * ImperialX Professional Architecture
 */

export { workerBridge, WorkerBridge } from './WorkerBridge';

// Worker is imported via URL in WorkerBridge, not exported here
