export const SemanticZoomController = () => {
    // Semantic Zoom is currently disabled in favor of explicit View Mode switching.
    // Future: Map Zoom levels to Free/Orbital transitions if desired.
    return null;
};
